@inject Radzen.DialogService dialogService

@if (Notifications != null)
{
    <RadzenTemplateForm TItem="List<NotificationsViewDto>" Data=@Notifications>

        <RadzenStack Gap="1rem" Orientation="Orientation.Vertical">
        </RadzenStack>

    </RadzenTemplateForm>
}
