@if (ScheduleForEventView?.Event != null)
{
    <MudDialog Style="width:550px; min-width:360px">
        <DialogContent>
            <MudStack AlignItems="AlignItems.Center" Class="mb-3">
                <MudCarousel @ref="Carousel" TData="PhotosForEventsDto" ShowArrows ShowBullets AutoCycle Style="width:300px; height:400px">
                    @if (ScheduleForEventView.Event.Photos != null)
                    {
                        @foreach (var photo in ScheduleForEventView.Event.Photos)
                        {
                            <MudCarouselItem Transition="Transition.Slide">
                                <MudImage Src=@($"/images/EventsPhotos/{photo.EventId}/{photo.Guid}/{EnumImageSize.s450x600}.jpg") Fluid="true" Style="border-radius:2%" />
                            </MudCarouselItem>
                        }
                    }
                </MudCarousel>

                @if (CurrentState.Account != null)
                {
                    <MudButton Color="Color.Primary" Size="Size.Small" StartIcon="@Icons.Material.Outlined.HowToReg" Variant="Variant.Outlined">Регистрация</MudButton>
                }
            </MudStack>

            @* Закладки *@
            <MudTabs Outlined="true" MinimumTabWidth="120px" Rounded="true" ApplyEffectsToContainer="true" PanelClass="pa-6">
                <MudTabPanel Icon="@Icons.Material.Filled.Info">
                    <Tab_About ScheduleId="@ScheduleId" ScheduleIdChangedCallback="ScheduleIdChanged" />
                </MudTabPanel>

                <MudTabPanel Icon="@Icons.Material.Filled.Message" BadgeData='"25"' BadgeColor="Color.Info">
                    <Tab_Messages ScheduleId="@ScheduleId" />
                </MudTabPanel>

                <MudTabPanel Icon="@Icons.Material.Filled.Group">
                    <Tab_Accounts ScheduleId="@ScheduleId" />
                </MudTabPanel>
            </MudTabs>

        </DialogContent>

        <DialogActions>
            <MudButton Color="Color.Primary" OnClick="Submit">Закрыть</MudButton>
        </DialogActions>
    </MudDialog>
}
